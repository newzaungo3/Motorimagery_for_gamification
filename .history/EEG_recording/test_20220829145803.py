from config import *
import os 
from pathlib import Path
'''if os.path.isdir(os.path.join(RECORDING_DIR, PARTICIPANT_ID)):
    print("we have folder")  
    
else:
    folder_path = os.path.join(RECORDING_DIR, PARTICIPANT_ID)
    Path(folder_path).mkdir(exist_ok=True) ''' 
    
print(f'EXECUTE_NO[EXE_COUNT]:02d') 